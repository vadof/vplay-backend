package com.vcasino.clicker.repository;

import com.vcasino.clicker.entity.Boost;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoostRepository extends JpaRepository<Boost, Integer> {
}
